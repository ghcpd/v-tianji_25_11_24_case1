Prompt

Problem Statement:
Build the UI for a Merino warm-layer e-shop. Create a small, modern, polished, aesthetically pleasing web UI project from scratch using Vue or React (your choice).

Requirements:

Create a complete runnable project from zero.

Generate full test coverage (unit + simple E2E).

All tests must run and pass.

Finally, run the project to ensure it boots successfully.

If anything fails, fix it and re-run until everything passes.

Do not assume success—treat it as a real execution environment.

Deliverables:

Full project structure

All source code files

All test files

All configuration files

Commands used to install, test, and run

Logs from running tests and launching the dev server

Short summary after successful run

Title:

Merino Warm-Layer E-Shop UI Generation & Full Test Execution

Description:

This task evaluates the model’s ability to build a complete, visually polished UI project from scratch, implement tests, execute them end-to-end, iteratively fix issues, and successfully launch the final application—treating the entire workflow as a real development environment.